<h2> Tell us something quirky about yourself </h2>

<div class="content-div">
  <%= form_for(@user, url: about_path(@user)) do |f| %>
    
    <%= f.label :about %>
    <%= f.text_area(:about,  rows: 10, cols: 30) %>

    <%= f.submit %>
  <% end %>
</div>
